# Structured logging, effective next sprint

All backend services must emit structured JSON logs by the end of Q3. The current free-text logs make incident triage slow and cannot be queried across services.

Owners: each service team migrates its own service. The platform team provides the shared logging library and a migration guide. Questions go to the platform channel.
